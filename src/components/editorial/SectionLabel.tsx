/**
 * Tiny section orientation marker — like a running header in a book.
 */
export const SectionLabel = ({ children }: { children: string }) => (
  <p className="text-label mb-element">{children}</p>
);
